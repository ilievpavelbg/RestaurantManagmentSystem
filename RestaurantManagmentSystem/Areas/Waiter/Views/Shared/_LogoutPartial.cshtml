@using Microsoft.AspNetCore.Identity
@using RestaurantManagmentSystem.Core.Data
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link disabled active" aria-current="page">Welcome <strong>@UserManager.GetUserName(User)</strong></a>
        </li>
        <li class="nav-item">
            <a class="nav-link disabled active" aria-current="page"> | </a>
        </li>
        <li class="nav-item">
            <a class="nav-link active" asp-area="default" asp-controller="User" asp-action="Logout" asp-route-id="@UserManager.GetUserId(User)">Logout</a>
        </li>
    }
</ul>
